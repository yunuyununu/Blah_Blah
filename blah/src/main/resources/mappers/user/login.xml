<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.blah.mapper.LoginMapper">
	<!--회원 로그인-->
    <select id="userLogin" resultType="com.example.blah.domain.LoginDTO">
		select u_id, u_password, u_nicname, u_email, u_phone
		from USER
		where U_ID = #{u_id} and U_PASSWORD = #{u_password}
    </select>
	
	<!--비밀번호 체크-->
	<select id="pwCheck" resultType="String">
		select IFNULL((SELECT U_PASSWORD from user where U_ID = #{u_id}), 'N')
	</select>
</mapper>
