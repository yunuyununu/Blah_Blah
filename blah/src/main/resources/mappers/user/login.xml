<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.blah.mapper.LoginMapper">
	<!--회원 로그인-->
    <select id="userLogin" parameterType="string" resultType="com.example.blah.domain.LoginDTO">
		select u_idx, u_id, u_password, u_nicname, u_email, u_phone
		from USER
		where U_ID = #{u_id}
    </select>
	
	<!--아이디 찾기-->
	<select id="searchId" parameterType="map" resultType="com.example.blah.domain.LoginDTO">
		SELECT U_ID from user where U_EMAIL = #{u_email} AND U_PHONE = #{u_phone}
	</select>
	
	<!--비밀번호 찾기 정보 검증-->
	<select id="searchPw">
		select u_idx, u_id
		from user
		where U_EMAIL=#{u_email} and U_ID=#{u_id} AND U_PHONE=#{u_phone}
	</select>
	
	<!--비밀번호 재설정-->
	<update id="pwReset">
		update user set U_PASSWORD = #{u_password} where U_IDX = #{u_idx} AND U_ID=#{u_id}
	</update>
	
</mapper>
